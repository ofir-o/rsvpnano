# Web Flasher Artifacts

This directory contains the browser-flasher image plus the published OTA binaries:

- `rsvp-nano.bin` is the full browser-flasher image for Touch LCD 3.49.
- `rsvp-nano-ota.bin` is the legacy app-only OTA binary for existing Touch LCD 3.49 devices.
- `rsvp-nano-esp32-s3-touch-lcd-3.49-ota.bin` is the board-specific Touch LCD 3.49 OTA binary.
- `rsvp-nano-rev2.bin` is the full browser-flasher image for Touch LCD 3.49 rev2/GPIO42 boards.
- `rsvp-nano-rev2-ota.bin` is the legacy app-only OTA binary for Touch LCD 3.49 rev2/GPIO42 devices.
- `rsvp-nano-esp32-s3-touch-lcd-3.49-rev2-ota.bin` is the board-specific Touch LCD 3.49 rev2/GPIO42 OTA binary.
- `rsvp-nano-esp32-s3-touch-amoled-2.41-ota.bin` is the board-specific Touch AMOLED 2.41 OTA binary.

For local exports, it is generated by:

```sh
python3 tools/export_web_firmware.py
```

The rev2/GPIO42 artifacts are for boards where the default firmware boots but brightness/backlight
control does not respond.

The generated `.bin` files are intentionally ignored by Git. In GitHub Pages deployments, the
site downloads these files from the latest published GitHub Release so the hosted web flasher only
serves official release builds.
